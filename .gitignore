node_modules/
data/*.db
data/*.db-wal
data/*.db-shm
raw/
.env
dist/

# The 9호선 spreadsheet is hand-downloaded and has no API to re-fetch it from,
# so unlike the cached API responses it is the only copy — keep it.
!raw/*.xlsx
